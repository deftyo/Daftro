services:
  daftro:
    build:
      context: .
      dockerfile: docker/Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - /mnt/c/Users/Matt/TaskList:/reports
    environment:
      - REPORTS_DIR=/reports
      - PORT=3000
    restart: unless-stopped
